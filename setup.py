from setuptools import find_packages, setup

setup(
    name='polytropica',
    packages=find_packages(include=['polytropica']),
    version='0.0.1',
    description='A Python 3 library for working with tropical polynomials',
    author='Gabriel Kelly',
    license='MIT',
)